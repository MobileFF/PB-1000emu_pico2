# 変更履歴

このファイルは `git` のコミットログとは別に、ユーザー向け・開発者向けの主な変更点をまとめた
ものです。日付は変更が行われた（おおよその）日を示します。GitHub へは変更のたびに直接
push される運用のため、バージョン番号や「未リリース」の区分けは設けず、作業のまとまりごとに
日付見出しで記録します。

---

## 2026-08-23

### 追加

- **BANK1/2/3 の動的メモリ確保**（`src/modhd61700.c`）: 拡張RAMバンク1〜3のバッファを、
  起動時に選択されたプロファイルが実際に持つ `ramN.bin` の分だけ MicroPython の GC ヒープから
  都度確保する方式に変更。使用バンク数が少ないプロファイルほど実際に空きヒープが増える。
  最初の実装は実機で起動不能（LCD真っ暗・REPL応答なし）になり、静的配列版へ一旦切り戻した上で
  原因を特定（CPUの毎バイトメモリアクセス経路が `has_bank[]` フラグのみでバッファポインタの
  NULLチェックをしていなかった）。この不整合が起きないよう確保順序を含めて修正した再実装版を
  実機で複数回の起動・長時間セッションにわたって確認済み。

### 修正

- 起動時のヒープchurn（`[mem_overlay]` の空きメモリ表示のギザギザ変動）を実機ログでの
  地道な二分探索により多数特定・修正。ディスプレイドライバの `set_window()`/`write_cmd()`/
  `fill_rect()` が毎回新規バッファを確保していた問題、ステータスバー描画のフォント辞書再構築、
  `PB1000System` の一部属性が `__init__` で初期化されておらず `getattr(obj, name, default)` が
  毎ループ `AttributeError` を発生させていた問題、PIO UART/CPU実行ループのバウンドメソッドが
  毎回再生成されていた問題など。合計で秒間十数KB規模あった変動を、実質ゼロに近い水準まで削減。

---

## 2026-08-22

### 削除

- **UART1 経由のシリアルコンソール・キーボード入力を完全に削除**（GP4/GP5、
  `[keyboard] enable_uart_kbd`）。PIO UART 経由の仮想 RS-232C（GP6/GP13）とは別機能で、
  そちらには影響なし。
- **EMULATOR MENU の RAM Load（保存済みスナップショットの実行中復元）を削除**。
  Toggles サブメニューも Beep のみに縮小（他のトグルは起動時セットアップメニュー(F1)や
  `pb1000.ini` の設定項目に統一）。別プロファイル・別セーブに切り替えたい場合は、
  Reboot Emulator (MCU) 後にプロファイル選択画面から選び直す運用に変更。

### 変更

- コンパイル時ヒープ断片化対策として、大きめのファイルを機能単位で分割:
  - `pb1000.py`（1943行）→ 多重継承ミックスインで `pb1000_fdd.py`（仮想FDD関連）・
    `pb1000_state_io.py`（save_state/load_state・フックレジストリ）を分離。
  - `emulator_menu_ext.py`（704行）→ `emulator_menu_ram.py`・`emulator_menu_capture.py`・
    `emulator_menu_debug.py` に分割。
  - `main.py` の起動時 import を遅延化し、プロファイル選択画面や起動時セットアップメニュー(F1)
    の段階で `main_input`・`main_runtime`・`pb1000.py` 本体をコンパイル対象に含めないよう変更。
- `pio_uart.py` の RX/TXバッファを list+`pop(0)` からゼロアロケーションのリングバッファに
  書き換え。
- LCD文字描画の共通処理を7箇所の重複実装から `mp/draw_text.py` に統一。

---

## 2026-08-17

### 追加

- **拡張（ext）モジュールの自動ロードを RAM プロファイル単位に対応**（`mp/pb1000.py`の
  `_ext_load_modules()`）。従来は`/sd/ext/`・`/ext/`の2箇所のみを走査していたが、これに
  選択中のRAMプロファイル配下の`<プロファイル>/ext/`（例: `/sd/rams/FOREX_PB/ext/`）を
  最優先で追加した。特定のプログラム専用のパッチ的な拡張（例:
  `forex_pb_inkey_patch.py`をFOREX_PBプロファイルの読み込み時だけ有効にする）を、他の
  プロファイルには一切影響を与えずに導入できるようにするため。同名モジュールが複数箇所に
  ある場合の優先順位は「プロファイル別 > `/sd/ext/` > `/ext/`」。
  あわせて、この優先順位を`sys.path`へも正しく反映するよう修正（`sys.path.insert(0, ...)`を
  優先順位と同じ順で呼んでいたため、`insert(0,...)`の性質上、実際には優先順位が逆転して
  いた——同名モジュールが複数箇所に存在する場合にのみ顕在化する潜在バグで、今回3箇所目の
  追加を機に発見・修正した）。
- EMULATOR MENU の System サブメニューに **Reboot Emulator (MCU)** を追加
  （`mp/emulator_menu.py`、確認ダイアログ付き）。既存の **Reset** はエミュレートされた
  PB-1000 側のリセット（実機のNumLockキー相当）のみで、Pico本体（MCU）自体は再起動しない。
  今回追加した項目は`machine.reset()`でPico本体を実際にハードウェアリブートする、明確に
  別種の操作。フリーズ・ハングして実行状態からの復帰が困難な場合の手段として用意した。
  RAM Saveしていない進行状況は失われるため、確認画面を経てから実行する（NEW ALLと同水準の
  警告）。
- 起動時のプロファイル選択画面に **F12: REPLへ抜ける（ブートしない）** ショートカットを追加
  （`mp/boot_session.py`、確認ダイアログ付き）。新しく書き込んだ`main.py`に、起動後半や
  対話ループで初めて顕在化するバグがあった場合、`main.py`は毎回自動実行されるためREPL
  接続そのものが塞がれてしまい、BOOTSEL+`flash_nuke.uf2`でのフルワイプ以外に復旧手段が
  無くなる、という運用上のリスクがあった（このリスクを避けるため、これまで`main.py`は
  意図的に一度もフラッシュへ転送せず`mpremote run`でのみテストする運用にしていた）。
  ROM/RAMロードやCPU起動より前の、最も安全なこの画面でF12を押すと`SystemExit`を送出して
  `main()`を安全に抜け、REPLへ戻れるようにした。これにより`main.py`を実際にフラッシュへ
  書き込んでの検証もしやすくなる。
- **HDMIミラー出力機能**（目玉機能）: 第2の Raspberry Pi Pico 2 + HDMI出力アドオン
  （PICO-HDMI-PLUS 等）を追加すると、本体LCDの表示内容をHDMIモニターへリアルタイムで
  ミラー表示できるようになった。オプトイン設計で、アドオンを持たないユーザーの動作には
  一切影響しない（`pb1000.ini` の新設 `[hdmi]` セクション、デフォルト `enable=false`）。
  - 配線は既存のSPI1バス（LCD/SDと共有、GP10=SCK／GP11=MOSI）に、新設のCSピン
    （GP28固定を推奨、外部SPIデバイス用に唯一空いているGPIO）を追加するのみ。
    `baudrate`／`frame_skip` も `pb1000.ini` から調整可能。
  - EMULATOR MENU（GUI+F7 > Display > HDMI）からON/OFFの切り替え・保存が可能。
  - ゲーム画面（LCD VRAM／Color VRAM拡張）だけでなく、起動時のRAMプロファイル選択画面・
    EMULATOR MENU・ベゼル（枠装飾）も、それぞれ独立したレイヤーとしてHDMI側にミラー
    表示される。各レイヤーは受信側でのウィンドウ中央寄せ・前回描画領域のクリアを完全に
    独立して管理しているため、キャンバスサイズの異なる複数レイヤーが同時に存在していても
    互いの表示位置がずれたり、他レイヤーの表示を誤って消してしまったりしない。
    テキスト/矩形中心の画面（メニュー等）は生ピクセルではなく「矩形塗り」「文字列描画」の
    コマンド列として送信することで、送信側（本体）の処理負荷と転送データ量を抑えている。
  - 受信側ファームウェア（第2Pico 2上でHSTX経由のHDMI/DVI信号生成を行う）は、姉妹
    プロジェクト MSX_emu_pico2 とも共有する汎用ファームウェアとして、独立プロジェクト
    [`hdmi_bridge_receiver`](https://github.com/MobileFF/hdmi_bridge_receiver)
    （MITライセンス）として公開。プロトコル仕様・配線・ビルド手順は同プロジェクトの
    ドキュメント（日本語／英語）を参照（本プロジェクトの `hdmi_bridge/README.md` からも
    リンクしている）。

### 修正

- `mp/sdcard.py`・`mp/xpt2046.py`（タッチパネル）: SDカード／タッチパネルへのアクセス直前に
  行っていた`self.spi.init(baudrate=...)`が、ボーレートしか再設定しておらずSPI通信モード
  （CPOL/CPHA）は直前の状態のまま残ってしまう不具合を修正（姉妹プロジェクト MSX_emu_pico2 の
  `mp/sdcard.py`で発見された既知のMicroPython挙動——`polarity`/`phase`を明示的に渡さない限り
  `spi_set_format()`が呼ばれない——が原因。詳細は `references/sdcard_spi_mode_bug.md`）。
  今回追加したHDMIミラー出力（モード3で通信）が有効な状態でSDカード／タッチパネルへ
  アクセスすると、モードの取り違えにより読み込みタイムアウトやデータ破損、タッチ座標の
  誤読が発生し得た。全該当箇所（`sdcard.py`6箇所、`xpt2046.py`2箇所）に`polarity=0, phase=0`を
  明示的に追加し修正。HDMIミラー出力機能自体が今回初めて追加されたものであり、この経路は
  これまでのリリース版では一度も到達されていないため、独立した不具合修正としてではなく
  HDMIミラー出力機能の一部として記載する。
- `[hdmi]` セクションを**内蔵フラッシュの `/pb1000.ini` 限定**に変更（`mp/config.py` の
  `load_config()` が `/sd/pb1000.ini`・プロファイル別 ini の `[hdmi]` を丸ごと無視するように
  なった。あわせて `mp/emulator_menu.py` の HDMI トグルの保存先も常に `/pb1000.ini` に固定、
  `mp/setup_menu.py`（F1のBIOS風設定メニュー）の `[hdmi]` 項目もSD/プロファイルiniの編集時は
  非表示に）。実機検証時、EMULATOR MENUからのHDMIトグルが `/sd/pb1000.ini` に
  `enable=true` を保存していたため、後から内蔵フラッシュ側の `/pb1000.ini` を
  `enable=false` に書き換えても優先順位（`/pb1000.ini` < `/sd/pb1000.ini`）により
  SD側が勝ち続け、`enable=false` にしたつもりが実際には効かない、という不具合が
  実際に発生した。固定のハードウェア配線に関する設定がSDカードやプロファイルごとに
  変わりうる設計だったこと自体が原因のため、`[display]`/`[touch]` の一部キーと同様に
  「内蔵フラッシュのみ有効」な設定として扱うよう変更。
- 起動時の **RAM Profile 選択画面**で、プロファイル数が画面に収まる行数を超える場合に
  スクロールが機能しない不具合を修正（`mp/boot_session.py`）。`_draw_profile_ui()` は常に
  先頭のプロファイルから描画し、画面に収まらなくなった時点で単純に打ち切るだけで、
  選択位置（カーソル上/下で移動する`sel`）が画面外に出てもスクロール位置を追従させる
  仕組みが元々存在しなかった。そのため、プロファイル数が表示可能行数を超えている状態で
  カーソル上を押して末尾（一番下）のプロファイルへラップアラウンドした場合、選択中の
  項目がそもそも描画範囲外になり、ハイライトも一切表示されなくなっていた。EMULATOR MENU
  （`emulator_menu.py`の`_run_menu()`）と同じ「スクロールオフセットをカーソル位置に追従させる」
  方式を導入して修正。
- **起動時のプロファイル選択でも、EMULATOR MENU の RAM Load と同じ完全復元（PC・レジスタ
  復元、リセットなし）を行うように変更**（`mp/main.py`）。2026-08-13にRAM Load完全復元機能を
  追加した際、起動時の自動ロードだけは`system.load_state(restore_cpu_state=False)`を明示的に
  指定し、RAMのみ復元してPC=0x0000にリセットする保守的な挙動のままにしていた（無人起動——
  プロファイル選択画面がタイムアウトし誰も操作していない状態——で壊れたセーブを復元して
  ハングし続けることを避ける意図）。しかし実際には、この配慮がタイムアウト自動選択と
  Enterキーでの明示選択を区別せず一律に適用されており、明示的にプロファイルを選んで起動
  した場合も期待されていた「セーブした時点から再開する」挙動にならなかった。
  `load_state()`本来のデフォルト（`restore_cpu_state=True`）を使うよう変更し、起動時も
  EMULATOR MENUのRAM Loadと同じ完全復元を行うようにした。無人起動時の保険としては、
  起動後1.5秒以内にCPUがKEY_INT無効のままスリープしていないか確認し、該当すれば強制リセット
  する既存の仕組み（"Startup sleep detected"ガード）が別途存在する。
  - この変更に伴い、再開直後の画面がロード前の表示（黒画面）のまま更新されない不具合が
    判明・修正した。原因はタイミングの問題ではなく、**LCDハードウェア自体が持つピクセル
    バッファ（`src/lcd_controller.c`の`lcd_state.vram`）がRAM Save/Loadの保存対象に
    含まれていない**ことだった。PB-1000はLCDへのアクセスにHD61700の専用I/Oポート命令
    （STL/PPO/STLM等、`ram0.bin`へのメモリ書き込みではない）しか使わないため、`vram`は
    `ram0.bin`とは別のCバッファであり、`load_state()`は一切触れない。通常はROM自身の
    起動処理（PC=0x0000から）がこの`vram`を再構築するが、途中のPCから再開する場合は
    再開後のプログラムが自発的に何か描画しない限り`vram`は空のまま。
    一方、ROMがテキスト画面管理に使う`LEDTP`（`&H6201`-`&H6800`、通常RAM内・`ram0.bin`に
    含まれ正しく復元される）はROM本来のDOTDSルーチン（`&H022C`、`references/rom0.src`）
    により`vram`へ転送される。このDOTDSの転送ロジックを踏襲した`system.refresh_lcd_from_ledtp()`
    （`mp/pb1000.py`、CPUレジスタには一切触れない純粋なデータ移動）を新設し、`main.py`の
    起動時と`emulator_menu_ext.py`の`_do_ram_load()`（EMULATOR MENUのRAM Load）の両方で、
    `power_on()`直後・`force_full_redraw()`の前に呼ぶようにした。LEDTP転送ロジック自体は
    64ドットモード用のDOTDS置き換え（`mp/ext/dotds_64dot.py`の`_dotds_override()`）を
    参考にしており、そちらとの違いはSCTOP（スクロール位置）を正しく考慮する点（64ドット版は
    用途上SCTOPを無視する設計のため流用できなかった）。C側の変更は無く（既存の
    `lcd_c.blit_reversed()`/`get_num_pages()`/`mark_dirty()`を再利用）、Python側の
    変更のみで完結する。
  - 同種の欠落が**カラーVRAM（VDP拡張、`lcd_state.color_vram`）**にもあったため、あわせて
    対応した。モノクロVRAMと異なりカラーVRAMは`LEDTP`のような「通常RAM内に元データがあり
    そこから再構築できる」性質のものではなく（プログラムの実行に伴い`write_vram_pixel_byte()`
    が逐次スタンプするか、バンクRAM→カラーVRAM DMA、または`vram_loader.py`のSD/FDDローダー等で
    直接書き込まれる）、独自のファイルとして保存する以外に手段が無い。`save_state()`が
    VDP実際に使用中（`system.lcd.vdp_enabled`）の場合のみ`color_vram.bin`（12,288B）を
    保存し、`load_state()`が存在すれば読み戻して`vdp_enable`/VDP初期化済みフラグも復元、
    存在しなければ（VDPを使わない通常プログラム、または明示的にVDP無効化）VDPを無効化する
    ように変更（プロファイル切り替え時に前のプロファイルのVDP状態が残らないようにするため）。
    `lcd_c.get_color_vram()`が`lcd_state.color_vram`への直接参照（bytearray）を返す既存API
    のため、こちらもC側の変更は無い。

### ドキュメント

- `doc/usage_guide.md` / `_en.md`: §2「起動フロー」に、これまで未記載だった **F1: BIOS風
  設定メニュー**（`mp/setup_menu.py`。編集対象の選択・キー編集・Save & Exit時のリセット・
  Discard）と **F12: REPLへ抜ける**（開発・復旧用のエスケープハッチ）を追記。あわせて
  プロファイル選択画面が自動スクロールする旨も追記。「自動ロード」節を、起動時も
  RAM Loadと同じ完全復元を行うようになった実装に合わせて更新（旧挙動は補足として残す）。
  §8「ステート管理」の保存ファイル一覧に`color_vram.bin`（VDP使用時のみ）を追記。
- `doc/hardware_guide.md` / `_en.md`: 新設セクション9として、HDMIミラー出力の配線
  （本体側Pico 2 W↔受信側Pico 2、GP28 CS追加）と受信側プロジェクトへのリンクを追加。
- `doc/config_guide.md` / `_en.md`: 新設セクション14として `[hdmi]` の全キー
  （`enable`/`cs_pin`/`baudrate`/`frame_skip`）を追加。また §1に「例外2」として
  `[hdmi]` がセクション丸ごとフラッシュ限定である旨（`[display]`/`[touch]`の一部キーとは
  実装機構が異なる点も含め）を追記。
- `doc/usage_guide.md` / `_en.md`: 新設セクション11として、有効化方法とLCD/HDMI排他仕様
  （HDMI有効時はゲーム画面・ベゼル・RAMプロファイル選択画面・EMULATOR MENUのすべてが
  物理LCDではなくHDMI側にのみ表示される）を追加。
- `doc/emulator_menu_guide.md` / `_en.md`: Display サブメニューの表に **HDMI** 項目を追加。
  あわせて、メニュー構成図の Display の説明が「表示色の変更」のまま古くなっていた
  （以前のセッションで追加された LCD Height が反映されていなかった）のを
  「表示色・解像度・HDMI出力の設定」に修正（今回の機能追加ではなく既存の記載漏れの修正）。
  System サブメニューの表にも **Reboot Emulator (MCU)** を追加し、既存の **Reset**
  （エミュレートされたPB-1000側のみ）との違いを明確化する警告文を追記。
- `mp/pb1000.ini`: `[hdmi]` セクションのコメントが参照する見出し番号を、新設した
  `hardware_guide.md` セクション9に合わせて修正。あわせて、内蔵フラッシュ限定である旨の
  注記を追加。
- `doc/extension_api.md` / `_en.md`: `<プロファイル>/ext/`ディレクトリと3箇所の優先順位
  （プロファイル別 > `/sd/ext/` > `/ext/`）を追記。

---

## 2026-08-13

### 追加

- **RAM Load の実行状態フル復元**（目玉機能）: これまで EMULATOR MENU の **RAM Load** は
  RAM の内容だけを復元し、CPU は強制的にリセット（PC=0x0000）されていたため、プログラムの
  実行途中から再開することができなかった。**RAM Save 実行時点の PC・フラグ・全レジスタ・
  UA バンク・IRQ 状態までを保存し、RAM Load でそれらを完全に復元して、保存した瞬間の続きから
  そのまま実行を再開できるようにした。** リセットは行われない。
  - 起動時にプロファイルディレクトリから自動ロードされるステート（無人起動時の安全性のため）は
    従来通り RAM のみ復元して PC=0x0000 にリセットする、保守的な挙動のまま。完全復元は
    EMULATOR MENU から明示的に RAM Load を選んだ場合のみ行われる。
  - **既知の制限（不完全な部分）**: 仮想FDDの転送中などプログラム側が持つ周辺機器の状態までは
    保存対象に含まれていないため、その途中でのセーブは復元後に状態が食い違う可能性がある。
- EMULATOR MENU に **CPU Status** 画面を追加（PC・フラグ・UA・IA/IB/IE・IX〜KY・$0〜$31 の
  レジスタダンプ、PC 周辺の生バイト列、逆アセンブル表示。読み取り専用）。
- `_ext_load_modules()` が `.py` に加えて `.mpy`（`mpy-cross` で事前コンパイルしたもの）も
  自動ロード対象として認識するように修正。同名モジュールが両方存在する場合は従来通り `.py` が
  優先される。

### ドキュメント

- `doc/emulator_menu_guide.md` / `_en.md`: RAM Load が「リセットする」という誤った記述を
  修正し、実際の動作（リセットせず保存時点から再開）に合わせて更新。新規追加した CPU Status
  の項目を追記。また、**LCD Height**（32/64ドット切り替え、Display サブメニュー）は
  以前のセッションで実装済みだったにもかかわらずドキュメントに記載が漏れていたため、
  今回あわせて追記（このセッションでの機能追加ではなく、ドキュメント側の抜け漏れ修正）。
- `doc/usage_guide.md` / `_en.md`: 同様に RAM Load / 自動ロードの説明を実装に合わせて修正。
- `doc/extension_api.md` / `_en.md`: ext ローダーの `.mpy` 対応を追記。
- `doc/dev_guide.md` / `_en.md`: サブルーチンフック（call_hook）機構について、呼び出し経路
  （CAL直接／JP・JR直接／その他の汎用経路）によってスタックへの副作用が異なること、
  Python側の戻り値を無視して常に intercept する仕様であることを注記として追加。
- `doc/build_guide.md` / `_en.md`: 初期導入・ファームウェア更新時の `.mpy` の扱いについて
  未記載だったため追記。要点は次の3つ:
  1. デバイス上に同名の `.py` と `.mpy` が両方存在すると常に `.py` が優先されてしまうため、
     `mp/` を丸ごとアップロードしただけでは `.mpy` 版（`debug.py`/`keymap.py`/`pb1000.py`/
     `main_runtime.py`/`emulator_menu.py`/`emulator_menu_ext.py`）が無視される。`.mpy` を
     有効にするにはデバイス側の対応する `.py` を削除する必要がある——**「.py をコピー→
     該当分だけ明示的に rm→.mpy をコピー」という順序が必須**で、フォルダ単位で分けて
     コピーする方法（例: `mp/` の後に `mpy/` を上書きコピー）では `.py` の削除を代替
     できないため不十分（誤った初期案を訂正）。
  2. `.mpy` はコンパイルした `mpy-cross` と完全に同じ MicroPython バージョンのファームウェア
     でしか動作しない。バージョンが異なると該当モジュールの `import` だけが失敗する
     （ビルド・書き込み自体は成功するため気付きにくい）。
  3. ファームウェアを異なる MicroPython バージョンで再ビルドした場合は、`.mpy` も同じ
     `mpy-cross` で再コンパイルしてアップロードし直す必要がある。
  4. `main.py`/`boot.py` は `.mpy` 化できない。RP2 ポートの起動処理はこれらをモジュール名
     ではなくファイル名で直接チェックしているため、`main.py` が存在しないと `main.mpy`
     があっても起動時に実行されない。
  トラブルシューティングにも該当する `ImportError` の症状を追記。

---

## 記法について

このファイルは以後、まとまった変更があるたびに新しい日付見出しを先頭に追記していく想定です。
`git` のコミット単位の詳細な差分ではなく、**利用者から見て「前回リリース（実際に配布・
案内した版）との間で何が変わったか」が分かる情報だけ**を記載してください。

- **記載するもの**: 実際にリリースされた版から見て、機能・挙動・ドキュメントが変わった点
  （新機能、修正された不具合、ドキュメントの誤りの訂正等）。
- **記載しないもの**: 同じ開発セッション・同じ未リリース期間内で発生し、そのまま同期間内で
  解消した不具合や、リリースされたことのない内部実装（調査用のデバッグログ等）の追加・削除。
  これらは利用者が一度も体験していないため、「前回リリースとの差分」として意味を持たない。
  作業ログとして残したい場合はコミットメッセージ等、別の場所に記載する。
